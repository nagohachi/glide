# Vision (image + text) SFT. Run: `glide sft configs/examples/vision_sft.yaml`
# Model-agnostic: uses AutoProcessor + AutoModelForImageTextToText.
extends: ../base.yaml

task: sft
modality: vision

model:
  model_name_or_id: Qwen/Qwen2.5-VL-3B-Instruct   # any HF image-text-to-text model works
  auto_class: AutoModelForImageTextToText
  attn_implementation: sdpa

special_tokens:
  image_token: "<image>"
  image_pad_token: "<image_pad>"

data:
  # {"image": "/path/img.jpg", "prompt": "Describe.", "response": "...", "reference": "..."}
  train_jsonl_path: data/vision_train.jsonl
  eval_jsonl_path: data/vision_eval.jsonl
  image_field: image
  reference_field: reference

vision:
  enabled: true

template:
  train_on_completions_only: true
  response_template: "<|im_start|>assistant\n"

eval:
  generate:
    enabled: true
    max_new_tokens: 128
    batch_size: 4
  metrics: [rouge]

training:
  output_dir: outputs/vision_sft
  num_train_epochs: 3
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 16
  learning_rate: 1.0e-5
